<?xml version="1.0" encoding="utf-8"?>
<!--
  Touch feedback on a tab, and nothing else. The selected look belongs to the island, which is one
  view that slides between the tabs; a tab that painted its own selected capsule as well would show
  two of them for the length of the move. The mask is the island's shape, so a press is a capsule.
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <corners android:radius="100dp" />
            <solid android:color="#FFFFFFFF" />
        </shape>
    </item>
</ripple>
