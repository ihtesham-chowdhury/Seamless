<?xml version="1.0" encoding="utf-8"?>
<!-- Gestures and tips: three parts, each filled in by HelpActivity. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:title="@string/help_title"
        app:navigationContentDescription="@string/action_back"
        app:navigationIcon="@drawable/ic_back" />

    <ScrollView
        android:id="@+id/scroll"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingBottom="32dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <include
                android:id="@+id/section_player"
                layout="@layout/view_help_section" />

            <include
                android:id="@+id/section_shorts"
                layout="@layout/view_help_section" />

            <include
                android:id="@+id/section_library"
                layout="@layout/view_help_section" />
        </LinearLayout>
    </ScrollView>
</LinearLayout>
