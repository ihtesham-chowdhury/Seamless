<?xml version="1.0" encoding="utf-8"?>
<!--
  A frame around the preference list, so this tab has a title like the other two.

  PreferenceFragmentCompat builds its own view and has nowhere to put a toolbar, so the
  fragment inflates this and drops the preference list into it underneath.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/settings_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:title="@string/tab_settings" />
</LinearLayout>
